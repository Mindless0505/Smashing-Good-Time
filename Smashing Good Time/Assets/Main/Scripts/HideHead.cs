using UnityEngine;

public class HideHead : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
    //     if (isLocalPlayer) // or whatever your multiplayer uses
    // {
    //     Camera.main.cullingMask = Camera.main.cullingMask & ~(1 << LayerMask.NameToLayer("LocalPlayerHidden"));
    // }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
